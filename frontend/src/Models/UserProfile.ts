type UserProfile = {
  name: string;
  phoneNumber: string;
  address: string;
  city: string;
  postcode: string;
  country: string;
  username: string;
  birthdate: string;
};

export default UserProfile;
